export const isAllowed = (path, permissionId = 1, action, roleId = 3) => {
  // if (roleId === 1) return true;
  // if (roleId === 3) return false;
  // if (permissionId === 1) return false;

  // return spmMatrix?.[path]?.[permissionId]?.[action];

  return true;
};
